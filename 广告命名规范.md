# 广告 / 表单命名规范（2026-09-09 起）

> 给投广告的人看。按这个起名，线索一进 CRM 就自动落到对应业务员名下，
> 不用再等「攒够样本推断」，也不用管理员手动分。

## 规则只有一条

**表单名和广告名都以业务员代号开头，后面用 `-` 隔开。**

```
<代号>-<国家>-<主题/车型>-<日期或序号>
```

例子（都是已经在跑的）：

| 名字 | 系统认出的代号 | 归属 |
|---|---|---|
| `miles-哥伦比亚-904` | `miles` | daimenglong |
| `grant-几内亚` | `grant` | wanglincheng23 |
| `Cheryl-卢旺达` | `cheryl` | 2064026258 |

系统怎么认：取名字**开头连续的英文字母和数字**，转成小写，就是代号。
大小写无所谓（`Miles` = `miles`），中文、空格、`-`、`_` 都算分隔。

## 代号要先登记

CRM 顶栏「📣 线索分配」→ 最上面「业务员代号」→ 填代号、选人、点「登记代号」。
只登记一次，之后所有以这个代号开头的表单 / 广告都自动归这个人。

当前已登记：

| 代号 | 业务员 |
|---|---|
| `miles` | daimenglong@gmail.com |
| `grant` | wanglincheng23@gmail.com |
| `cheryl` | 2064026258@qq.com |

**新来的业务员**：经理在「👥 团队」邀请进来之后，顺手在这里给他登记一个代号，
他的第一个表单上线前就能自动归属，不用等。代号自己定，英文短一点就行（`tom` / `lisa`），登记后不要改。

## 表单名优先，广告名兜底

一条线索归谁，按这个顺序：

1. 「线索分配」页里对这个**表单**手动指定过归属 → 以手动的为准（临时换人跟用这个）
2. **表单名**开头的代号
3. **广告名**开头的代号

所以最省事的做法是**表单名带代号**。广告名可以只写素材（`图片` / `视频-经销商型`），
但如果一个表单要挂给不同人的广告，就把代号写在广告名上，表单名不要带代号。

## 老表单怎么办

`RW-Nammi01-…`、`AZ-B2B-QINPLUS-…`、`PL-…` 这些不按规范命名的老表单，
归属还是走原来的「线索分配」页规则（已经配好），不用改名。Meta 也不让改有线索的表单名。

## 检查有没有生效

「📣 线索分配」页打开，每个表单下面会写「按表单名代号自动归 xxx」。
如果写的是「名字开头是 xxx，但这个代号还没登记」，点旁边的「去登记」。
顶部黄条「N 条线索没归属」为 0 就是全分完了。
