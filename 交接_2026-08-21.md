# 交接：2026-08-21

> 接着 `交接_2026-08-20.md` 看。今天一整天在做「广告线索按表单分给业务员」，
> 顺带修了 8/20 回填时我自己犯的三个错。

## 一、今天最重要的一句话

**闭环第一次带着归因跑通了。** 实测点「🎯 合格」之后：

```
lead_qualified 事件 → QualifiedLead 回传 Meta → meta_status 200
identifiers: ["ph", "fn", "lead_id"]     ← lead_id 在里面
```

`lead_id` 意味着 Meta 能把「这条线索合格」精确接回**它自己投出去的那一条广告线索**。
8/20 之前全库 `fb_lead_id` 是 0，这件事根本不可能。

## 二、线索归属：按表单，不按国家

boss 定的规则是「一个表单只给一个业务员」。**按国家分是错的** ——
卢旺达同时跑两个表单，`RW-Nammi01` 归 daimenglong、`RW-B2B-QINPLUS` 归 2064026258。

**Meta 不提供「表单 → WhatsApp 号」的绑定**，7 条路全试过（见 CLAUDE.md）。
归属是**从已发生的对话反推**的：客户点「Chat on WhatsApp」被真实路由到某个业务员，
`contact_handlers` 自动登记，按表单统计分布，纯度 91%–100%。

现在的 8 条规则：

```
daimenglong@gmail.com   RW-Nammi01-HigherIntent-EN-202608-1   (自动 99%)
                        RW-Nammi01-HigherIntent-EN-202608     (自动 94%)
2064026258@qq.com       RW-B2B-QINPLUS-form-v1                (自动 91%)
                        AZ-B2B-QINPLUS-form-v1                (自动 96%)
                        PL-B2B-QINPLUS-form-v2-1              (自动 100%)
                        PL-B2B-songPLUS-form-v2-1-copy        (人工)
                        PL-B2B-songPLUS-form-v3               (人工)
                        PL-B2B-QINPLUS-form-v2                (人工，0 条线索)
```

**新建广告表单之后不用记着做任何事** —— 顶栏「📣 线索分配」打开就会显示
「N 个表单没归属、M 条线索没人跟」，攒够 3 条样本自动出推断，点「采用推断」即可。

## 三、我在 8/20 回填时犯的三个错（都已修）

| 错 | 后果 | 修法 |
|---|---|---|
| 按**表单填的手机号**匹配客户 | 造出 **74 个重复**，归因挂在从没说过话的号上 | 用 FB 自动消息正文里的表单号/邮箱反查真人 |
| 重号正则只认 `filled in your form` | 漏掉三分之二（`filled out` 占 1,174 / 1,922） | 改成 `/filled\s+(?:in\|out)\s+your\s+form/i` |
| 按 `ad_id` 反推表单 | 2 条错（广告改过表单，归属不同的人） | 用 `fb_lead_id` 回 Graph API 拿权威 form_id |

**共同点：用间接信号推断身份，然后只验数量不验正确性。** dry-run 跑了三轮都只看
「匹配了多少条」，没抽样人工核对。boss 说「多点点」，点开一个客户当场就发现了。

所以「274 个从没进过 CRM」是高估，**真实约 200 个**。

## 四、⚠️ 唯一没解决的：Edge Function 要部署

`fb-lead-webhook` 里三处 `void supabase...insert()` 没 await。**Deno Deploy 在
handler 返回后会杀掉未完成的 promise** —— 实测 8 条真实线索的 contact 建出来了、
`fb_lead_id` 也写了，但 `fb_lead_received` 事件一条没落库。而线索分配页和
`apply_lead_routing` 都靠 `payload->>'form_name'` 找线索，**没事件 = 永远分不出去，
而且完全静默**。

代码已改成 await（`conversions-api` 同款问题一并改），**但要重新部署才生效**：

```bash
npx supabase login
npx supabase functions deploy fb-lead-webhook --no-verify-jwt
npx supabase functions deploy conversions-api
```

不部署的话，新线索会继续丢事件。补救脚本：
`node scripts/repair-missing-lead-events.mjs --apply`（要 `FB_TOKEN` 环境变量）。

## 五、当前数据

```
带广告归因的客户 : 489
contacts 总数    : 9,337   （回填前 9,128 → 峰值 9,402 → 删掉 74 个重复）
按业务员         : daimenglong 352 · 2064026258 129 · dengrongc6 2
其中还没联系过   : 约 258
```

未联系名单在 `广告线索_按业务员_2026-08-20.xlsx`（274 人版，其中 74 个已合并，
真实约 200；要精确的重新导一次）。

## 六、下一步

1. **打那些电话** —— 这个不依赖任何技术工作
2. **部署两个 Edge Function**（见第四节）
3. **23 个成交复核** —— `待复核成交_2026-08-20.md`，一个都还没动
4. **`QualifiedLead` 攒够几十条后**去 Events Manager 建 Custom Conversion，
   广告组优化目标设成它。⚠️ 顺序不能反：自定义事件必须先发出去、Meta 收到了
   才会出现在下拉列表里
5. **egress 优化**（可选）：全量刷新 9,411 行里只有 10% 真正用到，
   改成「先拉 id+phone 再按需取」可省 62%（2.41 GB/月 → 0.93 GB/月）

## 七、资产 ID（沿用 8/20）

```
Business  : Sino Gear Power Technology LLC  995401959837699
Pixel     : 1028794893354914
Page      : Sino Gear Auto  1241950519005601
App       : CRMDataSource   1364804098472538   已 Published
SystemUser: cici  61593387932394   token 永不过期 · 11 scope
广告账户  : sino gear-1  1841314377093106  ⚠️ Meetsocial 代理商所有
```
