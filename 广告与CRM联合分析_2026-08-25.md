# Facebook 广告 × CRM 联合分析（2026-08-25）

> 接 `广告与线索转化分析_2026-08.md`（8/19）。那一版归因链是断的，国家靠手机区号硬拼。
> 这一版 `fb_lead_id` / `fb_ad_id` / `ad_name` / `form_name` 全部落库，是第一份真归因的分析。
>
> 广告数据：`~/Downloads/Untitled-report.csv`（campaign 级，2026-07-20 → 08-16）
> CRM 数据：Supabase，623 个带广告标识的客户 / 624 条 `fb_lead_received` 事件（截至 8/25）

---

## 一、归因链已经对上了

同窗口内（7/20–8/16），CRM 里数出来的线索数和 Meta 报告的线索数：

| campaign | Meta 报告 | CRM 实数 | 差 |
|---|---:|---:|---:|
| Rwanda Vehicle Leads | 294 | 290 | -1.4% |
| AZ-LEAD-BYD-QINPLUS-DMI-USED | 59 | 60 | +1.7% |
| RW-LEAD-BYD-QINPLUS-DMI-USED | 32 | 33 | +3.1% |
| PL-LEAD-B2B-QINPLUS-202608 | 24 | 21 | -12.5% |

**误差在 3% 以内（波兰样本小）。** 这说明 8/20 那次回填 + webhook 是可信的，之后所有按广告/表单切的分析都不用再打折扣。这是 8/19 那版做不到的。

---

## 二、广告端体检（对照行业基准）

| campaign | CTR(链接) | CPM | 频次 | CPL | 判断 |
|---|---:|---:|---:|---:|---|
| Rwanda Vehicle Leads | 1.96% | $0.64 | 2.09 | $0.22 | 全部达标，CPM 低到反常 |
| PL-LEAD-B2B | 1.75% | $11.13 | 1.71 | $4.29 | CTR 好，CPM 正常，CPL 贵 |
| AZ-LEAD-BYD-QINPLUS | 1.19% | $2.23 | — | $0.88 | 及格 |
| RW-LEAD-BYD-QINPLUS | **0.76%** | $1.05 | — | $0.66 | **CTR 低于警戒线（<0.8%）** |

基准：CTR 1–2% 合格 / 3%+ 优秀 / <0.8% 警戒；CPM $8–15 合格 / <$8 优秀；频次 1.5–2.5 合格。

**结论：广告投放本身没有技术问题。** 除了 `RW-LEAD-BYD-QINPLUS` 的素材点击率偏低，其余全部在合格线以上，非洲市场的 CPM 甚至低到几乎不要钱。**钱不是浪费在买流量上。**

---

## 三、全漏斗：花费 → 深度对话

用**消息数**做口径，不用 `customer_stage`（原因见第四节）。深聊 = 客户发过 ≥3 条消息。

| campaign | 花费 | 线索 | CPL | 我方触达 | 客户开口 | 深聊 | **每个深聊成本** |
|---|---:|---:|---:|---:|---:|---:|---:|
| Rwanda Vehicle Leads | $62.52 | 290 | $0.22 | 177 | 189 | 111 | **$0.56** |
| RW-LEAD-BYD-QINPLUS | $21.63 | 33 | $0.66 | 17 | 24 | 23 | **$0.94** |
| AZ-LEAD-BYD-QINPLUS | $53.05 | 60 | $0.88 | 19 | 26 | 24 | $2.21 |
| PL-LEAD-B2B | $90.14 | 21 | $4.29 | 9 | 11 | 11 | **$8.19** |
| **合计** | **$227.34** | **404** | $0.56 | | | **169** | **$1.35** |

三件事一眼可见：

1. **$227 买到 169 个真人对话，每个 $1.35。** 对汽车出口这个客单价来说，这个获客成本低得离谱。
2. **波兰每个对话 $8.19，是卢旺达的 15 倍**，且 0 议价、0 报价。占了 40% 预算。
3. **`RW-LEAD-BYD-QINPLUS` 是隐藏的性价比冠军** —— $0.94/深聊，仅次于主力，但只花了 $21.63（9.5% 预算）。它 CTR 低（0.76%）说明素材有问题，**但点进来的人质量高**。这是最值得加投的一条。

---

## 四、⚠️ `customer_stage` 已经不能当漏斗指标用

这是这次分析最重要的发现，影响所有基于阶段的报表。

**`fb-lead-webhook` 第 393 行给每条新线索默认打 `qualifying`**（注释写着「FB lead 至少是 qualified（填了表）」）。后果：

- 8/21 后进来的 138 个广告客户，阶段分布是 `qualifying 58 / negotiating 45 / new 31`
- 其中 **46 个 `negotiating`/`quoted` 客户里，25 个（54%）一条消息都没有**，消息数中位数是 **0**
- 换句话说，「新线索议价率 33%」这个数字是假的 —— 真实深聊率是 29%，议价接近 0

同时销售已经在手工往回改（`negotiating→qualifying 人工` 6 次），说明他们也发现了。

**三个后果：**

1. 看板上的漏斗、阶段分布全部虚高，不能拿来汇报
2. `mapStageToFbEvent('qualifying') = 'Lead'` —— 阶段一旦被人工确认，虚高的阶段会变成 CAPI 事件发给 Meta
3. **最要命的：不能再用 `customer_stage` 当"合格线索"的判定依据**，那正是原计划喂给 Meta 的信号

**修法**：webhook 建客户时用 `new`，不要用 `qualifying`。填表不等于合格，这正是整个项目要解决的问题本身。

---

## 五、按广告排名（真名，非 ID）

口径同上，消息数不看阶段。历史 + 当前全量。

| 广告名 | 表单 | 线索 | 我方触达 | 深聊率 | 判断 |
|---|---|---:|---:|---:|---|
| 「video」 | RW-B2B-QINPLUS-form-v1 | 51 | 53% | **71%** | **质量冠军，加投** |
| 「video- short」 | AZ-B2B-QINPLUS-form-v1 | 87 | **30%** | 41% | 质量好，**但我方只碰了 30%** |
| 「1」 | RW-Nammi01-HigherIntent | 243 | 59% | 36% | 主力走量，维持 |
| 「2」 | RW-Nammi01-HigherIntent-1 | 110 | 44% | 33% | 中等 |
| 「3」 | RW-Nammi01-HigherIntent-1 | 62 | 40% | 32% | 中等 |
| 「5」 | RW-Nammi01-HigherIntent-1 | 21 | 38% | 38% | 8/21 新上，样本不足 |
| 「4」 | RW-Nammi01-HigherIntent-1 | 11 | **0%** | 0% | **一个都没联系过** |

⚠️ 修正 8/21 那版的结论：当时我按阶段字段判断「广告 3 议价率 20%、广告 2 只有 2.5%」，建议关 2 加 3。**用消息口径重算，两者深聊率是 33% vs 32%，没有区别。** 那个 8 倍差距是阶段污染造成的假象。**不要按那条建议关广告 2。**

真实的广告级差距远小于表单级差距：**`RW-B2B-QINPLUS` 表单 68% 深聊 vs `RW-Nammi01` 表单 33%**，差 2 倍。差别在车型和话术（B2B 二手秦 Plus vs Nammi 01 新车），不在创意。

---

## 六、真正的瓶颈：我方触达率 48%

623 个付费线索里，**我方主动发过消息的只有 48%**。分国家：

| 国家 | 线索 | 我方触达 | 客户开口 | 深聊 | 首响中位 |
|---|---:|---:|---:|---:|---:|
| 卢旺达 | 399 | 54% | 61% | 43% | **0.1h** |
| 阿塞拜疆 | 71 | **34%** | 49% | 46% | **17.7h** |
| 波兰 | 24 | 46% | 54% | 54% | 15.2h |
| 坦桑尼亚 | 10 | 100% | 100% | 50% | 0.1h |

- **卢旺达首响 0.1 小时（6 分钟）是真人回的**，不是自动回复（出站消息 133 条 manual / 2 gem / 2 gpt）。这块做得很好。
- **阿塞拜疆首响 17.7 小时**，比 8/19 那版的 21.1 小时只改善了 3 小时，仍然是时差没解决。而 AZ 的深聊率（46%）比卢旺达（43%）还高 —— **这是一批质量更好、却被晾着的客户**。
- 触达率低不是"客户不回"，是"我们没发"。

**每个深聊 $1.35 的获客成本下，触达率从 48% 提到 80% 相当于预算凭空增加 66%，且不花一分钱。**

---

## 七、回传现状：管道通了，信号是空的

8/21–8/24 四天：CAPI 成功 168 条、失败 0（8/20 的 20 条失败是 token `code 190/subcode 464`，已解决）。

但拆开看广告线索的 155 条：

| 事件 | 条数 | 优化价值 |
|---|---:|---|
| `Lead` | 91 | ❌ 每条线索自动发的回声，无判别力 |
| `DisqualifiedLead` | 11 | ⚠️ 负样本 |
| `InitiateCheckout` | 6 | ✅ |
| `AddPaymentInfo` | 1 | ✅ |
| **`QualifiedLead`** | **1** | ✅ **唯一的正样本** |

**623 个广告线索，累计只判定了 16 个，其中合格 1 个。**
**而深聊 ≥3 条、随时可以判定的存量有 231 个。**

学习期门槛是 50 条/周，`QualifiedLead` 现在是 1.75 条/周，差 28 倍。**按这个节奏永远切不了优化目标。**

---

## 八、行动清单

### P0 — CRM 侧，不花钱，本周

1. **`fb-lead-webhook` 建客户改回 `new`**（第 393 行）。不改这个，阶段数据会持续污染，且会往 Meta 发假事件。
2. **判定那 231 个深聊存量。** 一人一天 30 个，一周做完。**必须包含合格的** —— 现在 16 条判定里 15 条是"不合格"，Meta 收到的全是负样本，学不到任何东西。
3. **阿塞拜疆排班。** 34% 触达 + 17.7h 首响，而它的深聊率是所有市场最高。一个人早班覆盖 AZ 时区就能解决。
4. **广告「4」的 11 个线索一个都没碰过**，直接分给人。

### P1 — 回传侧，一周内

5. **上 `EngagedLead` 自动事件**（≥3 条入站消息，规则判定，不用 AI）。按 33% 命中率 × 30 条/天 ≈ **63 条/周**，一次性过学习期门槛。这条不依赖任何人的执行力。
6. **给 `triggerFbConversion` 加 `hasAdIdentifier` 门槛**，停止把老客户簿的事件灌进 pixel（历史已灌 3,100 条）。

### P2 — 广告账户（账户在 Meetsocial 手里，需要他们执行）

7. **停 `PL-LEAD-B2B-QINPLUS-202608`。** $8.19/深聊、0 议价、占 40% 预算。唯一证据确凿该停的。
8. **加投 `RW-LEAD-BYD-QINPLUS-DMI-USED`（广告「video」）。** $0.94/深聊、71% 深聊率，只花了 $21.63。从 $21 加到 $60/月。
9. **换 `RW-LEAD-BYD-QINPLUS` 的素材。** CTR 0.76% 低于警戒线 —— 点进来的人很好，但点的人太少。这是全账户单点提升空间最大的地方。
10. **不要动 `Rwanda Vehicle Leads` 的四条广告（1/2/3/5）。** 它们之间没有实质差距，之前的排名是阶段污染造成的假象。
11. **预算规划提前量**：切成合格线索优化后，广告组预算需要 ≥5× 目标 CPA。现在是 $5/天。按 CPL 涨到 $1–2、合格率 30% 估算，单个合格线索 $3–7，**广告组日预算需要 $15–35** 才出得了学习期。现在的预算量级撑不住转化优化。

### P3 — 两周后

12. `EngagedLead` 量过 50/周之后，Events Manager 建自定义转化 → 广告组优化目标切成 Conversion Leads。**顺序不能反**，事件必须先发出去 Meta 才认。

---

## 九、一句话

**广告没问题（$1.35 一个真人对话），归因链修好了（误差 3%），回传管道通了（0 失败）。
现在卡在两件事：52% 的付费线索没人联系，623 个线索只判定了 16 个。
这两件都不在 Meta 后台，都在自己这边。**

---

## 十、口径说明

- 深聊 = 客户发过 ≥3 条入站消息，已排除广告表单自动首句
- 花费窗口 7/20–8/16，CRM 全量到 8/25，第三节做了窗口对齐，第五节没有（是全量排名）
- 8/17 之后没有广告花费数据，需要新的 Meta 导出才能算最近一周的单位成本
- `customer_stage` 因 webhook 默认值污染，本文所有漏斗均未使用该字段
- 0 成交仍需保留怀疑：`quotes` 表全库 1 条，线下成交大概率没进系统
