# CRM → R08 插件 → Chat 接入修复（2026-09-23）

## 实测故障

从 WhatsApp CRM 的测试客户 David（+8613552592187）选择「R08 技能试点 · Miles」，点击「生成」。旧扩展打开 ChatGPT 后自动切到 Work，仅填入 `@sino gear r08 miles`；CRM 返回「技能输入准备失败，未发送客户上下文」。没有发送 WhatsApp 消息。

当前 @ 菜单同时存在同名插件和旧 Skill。旧代码只接受 `skillMention` 和 32 位旧 ID，并把注入函数错误丢成通用失败，导致选择器阶段停止。

已核实的 Menglong 账号身份：
- 旧 Skill ID：`6aafbaf4f53c8191a8d6ff2bf5d9812a`
- 新插件显示名：`Sino Gear R08 Miles`
- 新插件详情/原生标签已核实 ID：`plugin_5e838f5f90dc81919776e122e642836e`
- 新标签：`data-symbol="ecosystemMention"`、`data-id="plugin:plugin_5e838f5f90dc81919776e122e642836e"`

## 修改

- 接受新插件 ID 和大写显示名，兼容旧 Skill 配置。
- 新插件使用 Chat，不自动切 Work。模板可选固定 High；未勾选时保留原设置。
- 用原生菜单插件图标标记区分同名插件与旧 Skill；选择后及发送前均核对不可变 ID 和标签类型。
- 核对完整上下文保留后才允许发送；不匹配时停止，不向错误插件填入客户内容。
- 注入函数返回结构化错误，使 CRM 显示真实原因。
- 模板表单接受新插件 ID，并说明 Chat 入口。

## 验证

- `npm run test:gpt-skill`：74 项通过，包括同名旧 Skill 共存、错误身份阻断、长文本保留、Chat 切换、模板保存、首次/续聊路由及客户隔离。
- `npm run build`：通过，现有 Vite 体积/动态导入警告不阻塞。
- 已构建本机 `extension/dist`，没有运行 `npm run package` 或推送团队强制升级。
- 本机已重载修复版本；2026-09-23 12:57 UTC 通过 CRM 模板 UI 保存新插件 ID/显示名与 Chat 描述，保留 9217 字批准知识和默认设置。
- 首轮实测通过：David 测试联系人 → CRM「生成」→ 自动打开 Chat 并选择原生插件 → 自动提交约 5.68 万字符的业务知识、历史消息和工作记忆 → 回复回到 CRM。
- 观察时间（手工采样，不是基准测试）：点击后约 36 秒已看到 Chat 回复；约 51 秒 CRM 已显示可复制的客户回复及中文翻译；稍后工作记录保存结束。
- 首轮使用同一 ChatGPT 网页请求取回正文和跟进元数据；未发送 WhatsApp 消息。测试会话：https://chatgpt.com/c/6ab3ccf7-a50c-83ea-b3c7-5a4f27b83c0e
- 首次讨论续聊成功复用同一会话并回填，但质量不通过：要求两句改稿，却返回长分析。查明 `buildDiscussionMessage` 在用户指令后追加“NOT a customer reply request”和强制战术分析，与用户要求冲突。
- 已去掉冲突：讨论输入按实际意图支持分析或改稿；改稿返回标准回复/翻译，强调句数和不新增承诺。CRM 对讨论中返回的明确三段格式使用正常回复卡片，纯分析保持内部展示。
- 新增结构化改稿和纯分析展示测试，相关共 111 项通过（76 技能/表单/路由 + 19 知识/工作流 + 16 语言/模块选择）。重新构建通过，已在扩展管理页点击重载。
- 讨论提示首轮复验已能正常回填回复卡，但仍未遵守“两句以内”，且默认跟进要求覆盖了本轮不改任务的范围。进一步在提示末尾重申当前要求；明确要求不创建/更新/调整跟进任务时不请求跟进元数据，并在保存层阻止任务写入。
- 用户选择固定 High。实测仅改网页设置不会改变旧会话重新打开时的档位，因此新增模板选项，`thinkingEffort: high` 随个人模板保存，在同一发送标签页用原生控件选择 High。已通过 CRM UI 为试点模板勾选保存。
- High 首轮菜单适配失败时已阻止发送；补充 Radix pointerdown 事件及菜单加载等待后重新构建重载。
- 最新测试：78 技能/表单/路由 + 17 响应完成与跟进保护，共 95 项通过；此前 35 项知识/工作流/语言测试通过。构建通过。
- 固定 High 已实测：CRM 自动打开的发送标签页显示 High，正文成功提交并回填同一客户/会话。严格“两句以内”仍未完全达标（含问候共三句），因此不能据此宣称回复质量全面通过。
- 本轮未生成跟进元数据，CRM 正常保存草稿且未附加跟进安排；保存层的禁止任务写入已由真实组件集成测试覆盖。
- 本轮遇到原 Chat 页仍显示 Stop，而另一个同会话页刷新后已显示 Copy response 的状态不一致；刷新原页后 CRM 顺利回填。此样本需人工刷新，后台完成稳定性仍是已知限制，不能称全程无人干预通过。

## 费用边界

用户已明确“免费”指现有账号内使用、不额外付费，并非零订阅账号。此修复使用现有 ChatGPT 的 Chat 网页入口，没有引入按量 API、Work 或 Pro 模型依赖，不购买额外额度。试点插件模板按用户要求固定 Chat High。现有账号的正常使用限额仍适用。

## Chat 模型与额度核查

2026-09-23 用户进一步要求不占 Pro 模型额度。官方说明：Chat 的 GPT-5.6 Sol 提供 Instant / Medium / High / Extra High；Pro 订阅含 Extra High。GPT-6 Pro 与 GPT-5.6 Sol Pro 另有专项额度，Chat 与 Work/Codex 使用额度分开。Thinking 自己仍有使用限制。用户最终选择 High；按此偏好设置试点插件模板，不自动升级 Pro。

来源：https://help.openai.com/en/articles/20001354

## 最后一轮 High 删改验收

从 CRM David 的讨论输入要求删除英文问候和报价承诺，只保留一句车型确认问题，附中文翻译，不写策略、不改跟进。2026-09-23 13:19:23 UTC 已自动回填并保存，未人工刷新。回复：

> Which Land Cruiser model are you looking for, LC300 or another version?

中文翻译：您需要的是哪一款 Land Cruiser？LC300，还是其他版本？

本轮成功保留同一客户/会话，正常显示复制和填入聊天框操作，无额外分析或跟进安排。没有点击填入或发送 WhatsApp。该删改样本通过；前述精确句数及一次页面完成状态卡住的限制仍保留记录，不以单个通过样本代替全面质量验收。

## Miles V2 官方迁移与默认入口

用户要求将 Miles V2 也迁为技能。Menglong Dai 账号原 GPT 页面显示未迁移 GPT 在 12 月 11 日后不可用，并提供原生迁移入口。通过该入口迁移后，页面显示成功，指令/知识文件与应用检查均无问题；新插件保持私有，后续更新在插件进行。

- 原 GPT：`g-6a2f7081d85c8191babfad41e1131be6`。
- 新插件：`Sino Gear Miles V2`，`plugin_bd1c07b572d08191882a68116ab63a17`。
- 详情：https://chatgpt.com/plugins/plugin_bd1c07b572d08191882a68116ab63a17
- CRM 原模板 `d8fefded-866e-40a9-a312-45af735dedbe` 原位切换为插件，保留名称「Miles V2 · Menglong」及 CRM 默认状态，固定 Chat High。
- 保存后重新打开编辑，确认插件身份/High/默认勾选仍在；原批准业务知识 2420 字与保存前逐字相等。其他账号模板未改。
- David 恢复自动匹配后自动选中 Miles V2。首次实测发现网页记住 Work，真实 Chat 控件为 `data-tpp-toggle-value="chatgpt"`，旧适配仅接受 `chat`。High 校验阻止了发送，未产生 Work 请求。
- 修复兼容两个 Chat 控件值，切换后重新查询元素；插件在仍选中 Work 时阻止填入，返回成功前再次校验。79 项技能/模板/路由测试通过，构建通过；本机已重载。
- 第二次复测发现输入框会先于 Chat/Work 切换控件加载，补充等待表面控件或普通 Chat 强度控件，避免在默认 Work 页面过早开始。增加延迟控件测试，最新 80 项通过，构建通过并再次重载。两次失败均未发送模型请求。
- 最终复测从记住 Work 的网页状态开始：13:36:29.648 UTC 在 David CRM 讨论框点击发送，自动转入 Chat High，实际提交 `ecosystemMention` 的不可变 ID 与 Miles V2 新插件完全一致，用户正文 37,943 字符，包含模板知识、客户历史和当前指令。
- 13:37:31 UTC CRM 自动回填并保存，约 62 秒，未人工切模式或刷新，后台临时标签页自动关闭。正常显示复制/填入按钮，英文为一句 `Which Land Cruiser model are you looking for, LC300 or another version?`，附对应中文翻译，无额外策略或跟进安排。没有点击填入/发送 WhatsApp。
- 会话：https://chatgpt.com/c/6ab3d603-1abc-83e9-921d-c9ac437314dc#sgc_skill=plugin_bd1c07b572d08191882a68116ab63a17
- 此样本验证迁移身份、默认路由、上下文传递、High、结构化草稿和自动回填；不等同于所有业务场景质量/性能已全面验收。
