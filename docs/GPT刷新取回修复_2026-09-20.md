# GPT 刷新后交付丢失修复 — 2026-09-20

## 现场证据

- 浏览器：Menglong；扩展 mjleiklkaailpmmclejegahkfnjhjkpj，来源为本仓库 extension/dist。
- 客户 Issa（@issa.bello300），contact_id `26ff8b30-c6fd-43c8-a533-dafad5a00eb2`。
- 原 request_id `02a72e31-3eb9-4a3c-bcc7-42633e7d69fe`；Chrome local 已有完成结果，replyProgress 仍为 generating。
- 原会话 https://chatgpt.com/c/6aafbde1-8048-83e9-8047-0a4a3229fadd#sgc_skill=6aafbaf4f53c8191a8d6ff2bf5d9812a ，页面中的客户 ID 与 CRM 匹配。
- 原回复内容为柴油四驱自动 FOB USD 17,400，询问数量以安排 Tema 运输。这里记录原生成结果，不代表本轮重新核验报价。

## 修复

GPTReplySection 在 GPT_RUN 前保存组织/客户绑定、请求号、模板与交付上下文。取回只查询原请求，不重新提交原 prompt；处理保留原报价计算、跟进和工作记忆路径。账号、本单和模板变更时拒绝自动归档，原记录仍可保留后解除等待。恢复记录未加载完成前禁用新生成。Web Locks 串行交付并重新检查原请求，避免原接收端与恢复端重复保存；跨崩溃的数据库多步写入仍不是事务，不能声称所有异常下严格 exactly-once。

## 现场恢复和范围

旧任务未存新版上下文，无法直接走新版完整交付。人工核对原聊天客户 ID 后，使用 Chrome DevTools Application → Extension storage 写入该客户 replyStatus 本机草稿，保留英文正文及中文策略，移除机器跟进标签并显示“跟进任务未自动补建”。未伪造来源条数/生成时间/AI日志。未补建数据库会话映射、工作记忆、日志或任务，用户可在恢复卡片打开原会话。

随后重载扩展，页面显示 Reloaded；刷新 WhatsApp，选中 Issa，实见恢复稿和可用生成按钮。无新的 GPT 请求，无 WhatsApp 填入/发送。仅加载本机 dist，没有发布团队安装包或变更 required_version。

## 验证

- node --test scripts/test-gpt-template-routing-integration.mjs scripts/test-gpt-run-delivery.mjs：49 项通过。
- 覆盖生成/讨论刷新恢复、客户隔离、失败解锁、模板变化拒绝与保留记录解锁、原任务失败提示，以及恢复完成后迟到的原接收端不重复保存。
- npm run build：通过。保留既有 Vite 大 chunk 提示。
- 实机仅核验旧结果恢复和扩展加载；未额外对真实客户生成一轮来验证新版完整恢复链路。
