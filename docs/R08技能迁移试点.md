# R08 销售技能迁移试点

### 2026-09-18 Miles V2 混动误拦截与单轮报价

实读Carlos的Miles V2对话6aaa5aa2-1578-83ea-96fb-b9cd77881a44：老板已补充“就是dg”，模型输入保留phev、运费11000含DG、地面已含，仍被计算器无条件PHEV拒绝；自动输入纠正再次调用GPT且变更标签/来源，最终客户稿仍为空。正常报价原来也强制“先空稿提取、再组织正文”，形成固定两轮。

改为完整稿/中文译文/报价输入/跟进判断同次生成；用公开USD金额占位符，由本地计算后立即替换，保留输入版本与费用校验。旧空稿及无效草稿仍允许补全，跟进元数据缺失仍可触发纠正，不能承诺任何情况都只调用一次。PHEV本单DG已确认且费用已含、地面已覆盖时正常算，保持phev，绝不重复加BEV费用；缺真实必要条件仍阻止。老板短句补充缺项延续原报价要求，不自动当作NO_REPLY。

验证：121项相关离线检查通过，包含生成/讨论两入口仅1个GPT_RUN、实际报价版本保存、118400计算、多方案金额映射、无效内部字段及未替换占位符不可对客。TypeScript/Vite构建通过。独立线上Miles V2测试 https://chatgpt.com/g/g-6a2f7081d85c8191babfad41e1131be6-sino-gear-miles-v2/c/6aad2b5b-7b84-83ea-91a4-c15e331c8287 单次输出完整西语稿/中文译文和合法quote_input。该测试是简化报价协议及获批测试数据，不是原Carlos全历史或CRM端到端新生成；未重新核验实际运价，未向客户发送或保存客户数据。没有修改GPT Heavy设置。

本机构建已更新extension/dist；原生Chrome控制停在扩展页菜单，Cancel/键盘等操作不改变状态且截图不可用，浏览器API无法接管chrome://。已请老板手动重载并刷新，尚未确认加载本次修复。此次未更新团队required_version、未全员发布、未改线上GPT本体/私人技能；公共CRM提示会在加载新版后的请求注入。


## CIF默认预算公式（2026-09-18）

老板确认燃油CNY2000/台、纯电CNY3000/台是所讨论附加费的合并预算；保险追问最终答复“总运费x1.1”。已按最终公式保存：CIF参考总额＝批准FOB车辆总额＋去重后的总运输费用×1.1；10%作为保险预算，不再另加最初提及的CNY1000，不乘车价。已含在FOB/供应商报价中的预算项目不重复加，BEV DG仍按适用柜数另计入运输基数，本单后续明确特批优先。该公式是老板批准的参考报价办法，不是已购买保险或已确认承运价格。

线上私人技能同ID已保存并重新加载核对，完整正文12358字符与写入值完全一致，旧“保险未授权而停报”段落已移除，两份Excel仍在。本地三套运费/报价/R08技能同步通过结构校验。CRM计算器新增insurance.basis=freight_10_percent，由引擎计算去重的海运、DG和人民币附加费预算总和的10%，保留固定保险金额覆盖与旧版本空保险输入。51项相关测试与构建通过（新增5台/6台、燃油费用换汇、BEV DG去重、固定覆盖及禁止两种保险叠加、最终整理流程）。Python报价辅助脚本同步支持公式并验证。Chrome扩展已重载看到Reloaded，WhatsApp已通过浏览器reload加载；未全员发版，未生成/发送此真实客户的新报价，未声称完成新的线上GPT端到端计算验收。

源稿分为 `SKILL.md`（回复判断、格式和推进方法）与 `r08-business.md`（已批准业务知识）。ChatGPT 网页版当前采用合并正文；上传附件受浏览器文件权限限制。这里没有客户消息、账户密钥或实际发送工具。

## 原始配置资料持久化（2026-09-18）

老板纠正测试目标为 Didace 的插件对话 https://chatgpt.com/c/6aacdf83-ec00-83e9-991a-570d745cefd3 ，并要求后续客户的具体参数也自动查原表。此前 José 的 Custom GPT 测试不能代替这条插件对话验收。

现用私人技能 `6aabac4c1240819193bc311372c9d2ab` 已保存“具体配置查原始资料”规则及两份原始 Excel：`02【配置表】商用车国际KP16产品配置表-3.31(3).xlsx`、`皮卡总库存-20260901-含价格(3).xlsx`。通过 macOS 原生文件选择器完成上传，没有扩大 Chrome 扩展本地文件权限。保存后重载编辑器，完整正文 11,838 字符与写入值完全一致；下载包 CRC 通过，两份 xlsx 的 SHA256 均与老板原件一致。上方旧版“上传受限”不再代表当前状态。

规则覆盖具体参数按市场/排放/动力/驱动/配置/物料号查表、内部保留单元格出处、一般询价不堆配置、采购成本不外发、历史库存不冒充实时库存。技术资料不改变已批准售价；载重不用总质量减整备质量替代，不把四驱牵引力当载重增加。原件在本地技能目录保留并通过该目录 `.gitignore` 排除，避免采购成本进入 Git；线上下载包另存于用户 Downloads。

Didace 附件实测已完成：正确查到 `KP16配置表!AM:AP` 非洲欧二两驱/四驱 Standard、Comfort 的 `≤625kg`（不含乘客），准确引用 `AP20=1910`、`AP21=3070`，发现没有欧二 Luxury 列，不能套用墨西哥欧六 Luxury；库存表第7–12行只支持欧二两驱历史记录。没有承诺日常载一吨，没有泄露采购价，也没有改报价或生成执行块。这里是正确识别已有参数和资料缺口，不代表本单 Luxury 欧二承载已确认。

另开无附件的技能调用对话验证跨客户取资料：https://chatgpt.com/c/6aad024d-8618-83e9-92e2-813918e9e0d6 。只给两组车型问题，不提供数值；结果记录见本地 `分析导出/R08配置与成本_20260918/`。本轮更新线上技能及本地技能源稿，没有变更扩展代码、全员升级版本或客户发送状态。

无附件新对话最终通过：实际读取技能目录的两份Excel，AO/AP重量口径及Z/AB座椅、主副驾调节、方向盘差异均与本地独立查表一致，不将皮革/仿皮写成真皮，不输出采购价或CRM执行块。新规则及资源持久化已验证；这不表示本单欧二Luxury适配已确认，也不代表未调用此技能的旧Custom GPT同步更新。本地技能结构校验通过，原件不进入Git。

2026-09-17 创建的当前私人技能：
https://chatgpt.com/skills/editor/6aabac4c1240819193bc311372c9d2ab

原 GPT 保留，CRM 正式 R08 模板尚未改为技能。旧试验技能 `6aaba8de0be08191b034678f93a971c5` 已删除并由上述完整版本重建；旧测试聊天仅作为历史验证记录。

## 验证边界

- 网页运行使用真实技能提及标签 `data-symbol="skillMention"`，发送前核对不可变 `data-id`；模型为账号当前 GPT-5.6 Sol Light。
- 初版验证知识读取、单台与合运报价、客户明确只买一台及暂停等待；当前定稿连续7轮验证主动询问意向、完整合运比较、CIF缺保险、切换西语、内部知识核对、完整网址以及暂停等待。逐轮记录在 `分析导出/R08技能迁移实测_2026-09-17/`。
- 早前ChatGPT技能预览及下载出现 `File not found`，重装/重建当时未解决。随后老板在场复查，预览已恢复，下载提示 `Download started`，独立新对话知识读取通过；文件落盘完整性尚未核验，详见文末更新。
- 编辑器正文是虚拟化渲染。`innerText` 可能只有视窗内一部分，绝不能读可见片段后覆盖全文。以后编辑须从完整源稿生成；首次出现片段覆盖后已用完整源稿恢复并重建。
- 浏览器准备发送的完整源稿去空白 FNV-1a UTF-16 哈希为 `b56282a8`，与本地两文件合并结果一致。这验证源稿一致，不替代服务端保存内容的读取验证。

## CRM 试点

管理模板增加“使用已安装的 ChatGPT 技能”，输入网页显示名称与编辑页末尾 ID。新模板保留完整 CRM 批准知识，旧模板不覆盖。技能 metadata 使用版本 2；旧客户端会明确拒绝不支持的知识格式，不会静默当普通聊天调用。

每次生成和内部讨论都读取当轮知识中的技能配置。新对话先选择真实技能标签再填上下文，续聊仍核对标签。找不到、重名、身份不符、正文截断或标签丢失时停止。对话保存的 `#sgc_skill=...` 是 CRM 自己的身份标记；打开网页前移除，绝非 ChatGPT 官方调用参数。

原 GPT 的聊天不续接到新技能。只有同客户、同模板、同技能的普通 `/c/` 对话才续聊。结果抓取仍使用当前 assistant 消息锚点及当前回合完成按钮，保留三段标题和空回复。

代码验证：`npm run test:gpt-skill`、原有知识/路由/段落测试、`npm run build`。这些是离线代码测试和构建，不等于已加载扩展的真实 CRM 全流程测试。

2026-09-17用户加载后，已仅用测试号码 **13552592187** 完成真实CRM生成、续聊、内部讨论、完整资料链接回读、ASK_BOSS/NO_REPLY空正文保护及回填预览，未发送消息。个人模板“R08 技能试点 · Miles”（`73bd63c7-44c4-4c16-b6f9-259fd2e68cc8`）已保存，默认模板和原R08正式模板不变。真实5轮会话：https://chatgpt.com/c/6aabd9a6-b8a4-83ea-9326-d251b4d89d5e 。正式全员发布尚未执行。

## 资料链接兼容修正

真实网页观察到部分命名链接只有文件名和点击处理器，DOM 不含 href，CRM 无法据此读取网址。首次/续聊回复与内部讨论现统一要求显式完整网址；纯文本网址已实测可正常读取，已有 href 的旧GPT链接路径继续保留。最终相关80项离线测试及构建通过，包含新增实际模板表单回归。


## 联调发现与后续维护

- 新建技能模板按钮不能要求隐藏的旧GPT URL；已修复并用真实React表单验证新技能创建和切回旧GPT时的URL要求。更新后的dist/试点zip已包含此修复，已保存试点模板可继续运行；按钮修复在下次重载后生效。
- ClientRecordCard原有自动保存会写入模拟档案。本轮首轮测试的3字段/3标签已精确清理并复核；以后模拟统一空档案段，若要测档案提取须先完整备份授权测试联系人的对应字段/标签，结束后逐项恢复。
- WhatsApp清空草稿须用实际输入事件并复核，不能仅根据fill('')调用成功判断；本次最终全选删除，未发送。


## 现场复查更新（2026-09-17，老板在场）

本次未修改、重装或重建技能，重新打开同一技能（6aabac4c1240819193bc311372c9d2ab）复查。

- **预览恢复**：从Skills列表打开已安装技能，SKILL.md详情正常显示完整规则、K01—K11批准知识和末尾资料链接，未再出现File not found。不能继续把此前故障写成当前仍存在。
- **下载启动正常**：技能编辑页More → Download后，网页提示Download started；本次未再报File not found。Chrome下载管理页被浏览器安全策略阻止访问，未绕过，因此仅确认网页启动下载，未核验落盘文件完整性。
- **独立新对话验证正常**：从Try in chat建立全新对话，核对真实skillMention ID与该技能一致。提示只给6个问题，不附批准知识或答案；实际回复正确给出柴油手动两驱14,700、EV505四驱26,400、70%离港前尾款、1,000转接头、300充电桩、一年三大件店保及完整空调声明样本URL。
- 验证记录：https://chatgpt.com/c/6aabea77-b060-83ea-8f56-cae2307e6dfe 。页面保留给老板查看。本次没有操作CRM客户、修改模板或发送WhatsApp消息。
- 根因未确认：本次未改配置即恢复，只能确认当前现象，不能断言平台已永久修复或此前错误的具体原因。


## 下载包最终核验（老板提供本地文件）

用户提供 `/Users/yang/Downloads/sino-gear-r08-miles.zip` 后，直接核验该文件：12,871字节，ZIP CRC全部通过，含SKILL.md、agents/openai.yaml、assets/icon.svg。SKILL.md去除frontmatter后，与本地已核对的ChatGPT完整正文忽略空白全文一致，没有内容缺失。SHA256：`553b923dec40aa76e3d69fec123d0e52fc086231a169db6bba73d7bf130c43b3`。

因此已确认下载文件完整落盘，之前“未核验落盘完整性”的限制现已解除。当前预览、下载包及独立知识调用均验证正常；历史File not found未复现，具体原因仍未知。本次只读取用户提供的文件，没有执行包内内容或改变正式模板/全员发布状态。

## 防重复改造与历史回放（2026-09-17，后续更新）

当前私人技能新增内部指令识别、累计要求保留、客户条件隔离和费用来源核对。已保存并重新加载编辑器回读全文9890字符，与预期文本忽略空白一致；上方历史下载包哈希不代表这次更新后的包。

真实网页GPT-5.6 Sol Light运行5个测试对话、13轮，覆盖10类目标问题。费用首测失败，改为先核对费用来源、删除无授权旧AI预留后，相同输入独立新对话及换数字变体通过。完整输入输出、首次失败、判定与限制保存在`分析导出/R08防重复回放_2026-09-17/验收结果.md`及同目录`完整回放.md`。

`extension/src/lib/gpt-sales-workflow.ts`统一首次回复、续聊与内部讨论规则；`gpt-prompt.ts`移除把所有销售指导强制用于客户正文的冲突措辞。33项相关离线回归、构建、技能结构校验通过。该CRM代码本地构建完成，尚未验证重新加载后的真实CRM路径，不等于全员发布。此次没有实现客户状态跨新对话持久化，也没有写测试客户档案或发送消息。


## 本单持久化与运费入口修正（2026-09-17后续）

早前“没有实现跨新对话持久化”为当时状态。后续已完成并实测CRM保存条件→清除会话→新会话恢复原报价，测试号记录已精确清理，原会话已恢复，未发送消息。见[客户记忆与运费实施](客户记忆与运费实施.md)。

老板否定运费填写页面，已从代码撤销。最新要求：通过对话自己查3–5家公开来源，同口径最高可比价估算；纯电港杂3000、油车2000人民币/台，已含不重加。该规则已追加到现有R08网页技能，保存后重载编辑器回读确认；本地源稿同步。CRM通用提示及内部研究记录自动保存已构建，浏览器最终重载联调单独记录。
