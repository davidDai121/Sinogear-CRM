# 网页回复优化：Claude 定点复核（2026-09-18）

老板已取消 API 方案，因费用不合适。沿用 ChatGPT 网页；不要配置付费 API、部署代理或恢复后台自动跟进。老板提示 Claude 约 38 分钟后额度重置，Codex 已通过桌面「浏览器问题追踪」同步分工。本轮实现由 Codex 完成，Claude 暂停，老板随后要求不等额度，Claude 已完成两轮复核，结论见 `网页回复优化_Claude复核结果.md`。

## 请只读复核这几处，不重复实现

1. `gpt-prompt.ts`：语言证据只对已经在本轮 Chat History 完整出现的长入站消息提供前 240 字 + fullTextRef。主历史附同一个原始 message ID，保留全文。检查长消息末尾显式语言要求仍可读；历史最后 50 条之外的语言证据必须保留全文；广告/出站/媒体过滤不变。
2. `gpt-followup.ts`：主上下文中已呈现的完整长销售指导，只在 contactId/scopeId/记录 ID/原文都匹配时用 fullTextRef；备注也要求全文相同。校验、状态哈希和保存使用完整原始上下文。独立跟进默认全文，不引用不存在的当前上下文。
3. `gpt-followup-result.ts` 与 `GPTReplySection.tsx`：互动生成/讨论传 repair=null。缺跟进块时立即保留草稿，显示未保存跟进提示，不自动追加 GPT 调用或改人工任务。合法跟进仍正常保存；机器块放进客户正文仍阻止；报价必要纠正保持原流程。
4. `gpt-sales-workflow.ts`、`sales-work-memory.ts`、approved-knowledge 提示头只精简重复说明，不删 owner 原话/客户问题/报价范围/运费日期。是否遗漏了实际业务决策必需的规则，是否反而让客户话变成格式腔？无需讨论 API。

## 已完成验证

- 217 项销售/GPT/事实/报价/运费回归通过；随后新增生成与讨论的缺跟进组件验收，组件套件 35 项通过（与 217 项有重叠，不相加）。类型检查及 Vite 构建通过。
- Didace 同一 DB 快照离线重放，均包含完整客户问题；未加入个人偏好和上次跟进计划的简化审计上下文：92,515 → 85,136 字符，减少 7,379（约 8%）。不是实际线上调用日志，也不是 token 下降值。
- 输入与报告保存在忽略目录 `分析导出/网页提示词精简_2026-09-18/`，不要提交客户原文。
- 本机扩展详情已核对路径 `extension/dist`，点 Reload 并刷新 WA。Didace 已实机后台自动返回：18:42，1 次调用、197.2 秒、85,742 输入字符、4,042 输出字符。八项问题全覆盖，保修/AT包/PI都保留。未手动激活 GPT，未填入/发送客户消息。
- 未发客户消息、未 package/更新 required_version、未 commit/push；无 API 调用。既有 webhook 改动不属于本轮。

## 仍未完成

彻底拆分回复/档案/跟进职责尚未实现。网页方案下贸然拆成三个模型往返会增加等待，因此这轮先减少重复并解除可用正文被跟进补块阻塞的问题。复杂单提示词仍大；没有宣称 80% 瘦身或保证回复几秒完成。

复核只需列真实缺陷及最小修法；没有问题就简短确认。不要扩大范围、开代理、重跑线上客户或重新 build。

## 实机语言问题，优先复核

18:42 结果有两句自我解释（“so I will not write guaranteed registration…”、“I will not state an insured value…”），完整性达标但表达仍偏防御。Codex 因此恢复压缩前已有的要求：缺项写成 PI 的实际下一步，不在客户话里解释自己如何避免错误承诺。最后这一句提示修订已跑语言/工作流回归并构建，未为同一问题再次消耗完整客户生成；不能声称语气已实机解决。无需再讨论数值精确性政策，复核重点是如何更自然地推进现有承诺。

原文快照：`分析导出/网页提示词精简_2026-09-18/Didace实机验收.json`。不要因为这次响应 197 秒、上次 261 秒就宣称稳定提速比例；上下文和远端状态不同。

## 本轮最终闭环

Claude 首审发现群聊续聊备注悬空引用：主 prompt 没有群聊备注，但跟进块误以为已呈现。Codex 两处调用改为群聊续聊传 null，四项组件回归覆盖生成/讨论 × 群聊/个人；Claude 二审确认已修。补了销售例句仅本单事实成立时可用的限制。事实库按完全相同来源/范围/日期合并引用，每条 statement/value 原样保留，Claude 二审通过。

最后相关回归 224 项通过，TypeScript/Vite 构建通过；本机扩展路径核对为 extension/dist，最终重载并刷新 WA。无团队 package/required_version/commit/push。

19:01 Didace 实机：后台自动交回，1 次请求，179.2 秒，输入 84,593 字符，输出 4,128 字符。完整覆盖八项问题，保留原报价、一年三大件保修、保养包和 USD 2,000 AT 包及 PI/30-70付款下一步。18:42 两句“我不会保证/不会先声明”的自我解释消失；改为当地代理确认注册要求和 PI 前确认保险范围。并不证明所有客户语气都已完美；本次没有手动激活 GPT，没有填入或发送客户消息。

最终离线同快照 92,515→83,868 字符（约 9.3%），实际在线 95,801→84,593 是不同时间点上下文，不作为受控速度比较。当前仍未完全拆分回复/档案/跟进职责；网页复杂单约 8.5 万字符，后续可继续按场景减少无关规程。
