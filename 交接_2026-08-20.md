# 交接：2026-08-20

> 接着 `交接_2026-08-19.md` 看。今天全天在两件事上：水单口径收尾 + **FB 归因链从头接通**。

## 一、今天最重要的一句话

**Facebook 广告 → CRM → 销售判定 → 回传 Meta 这条闭环，今天第一次真的通了。**

在此之前它有**五处独立断点**，每一处都足以让整条链路静默失效，而且从配置界面上一处都看不出来：

| # | 断点 | 现象 | 状态 |
|---|---|---|---|
| 1 | 旧 business 的 token | 错误码 190 | ✅ 修 |
| 2 | `FB_DATASET_ID` 指向旧 pixel | 100/33 对象不存在 | ✅ 修 |
| 3 | **Purchase 缺 currency** | subcode 2804010，**每条成交都被拒** | ✅ 修 |
| 4 | Page 和 pixel 分属两个 business | 拿不到线索表单 | ✅ 迁移 |
| 5 | **Page 从未订阅 leadgen** | 新线索根本不会推过来 | ✅ 订阅 |

第 3 条最阴险：**它跟 token 无关，换完 token 之后每条成交仍然会失败**。如果没有端到端真打一次事件，会以为修好了。

## 二、现在的资产清单（记住这些 ID）

```
Business  : Sino Gear Power Technology LLC  995401959837699
Pixel     : 合肥鑫齿动力科技有限公司's Pixel  1028794893354914
Page      : Sino Gear Auto                  1241950519005601
App       : CRMDataSource                   1364804098472538   已 Published
SystemUser: cici                            61593387932394     token 永不过期 · 11 scope
广告账户  : sino gear-1  1841314377093106   ⚠️ 归 Meetsocial 代理商所有，动不了
弃用      : Sino Gear Auto business 1244018702132116（主页已迁走，里面空了）
```

Supabase secrets 已配：`FB_ACCESS_TOKEN` / `FB_DATASET_ID` / `FB_APP_SECRET` / `FB_VERIFY_TOKEN` / `FB_ORG_ID`。

## 三、线索数据的真相（今天挖出来的）

8 个表单、**505 条线索、481 个唯一手机号，全部产生在 8/4–8/20（约 31 条/天）**。

- **274 个（57%）从来没进过 CRM** —— 花钱买来的线索超过一半蒸发了
- 进了 CRM 的 207 个里，**161 个（78%）是 `stalled`**
- 只有 6 个走到报价

已全部回填（`scripts/backfill-fb-leads.mjs`），481 个客户现在都带 `fb_lead_id` + `fb_ad_id`。
未联系的 274 人导出在 `广告线索_从未联系_274人_2026-08-20.xlsx`，按「多久买 × 要几台」排了优先级，**102 人标红**（30天内要买或要 6 台以上），**20 个潜在批量买家一个都没被联系过**。

## 四、下一步该做什么

**1. 打第一批电话** —— 那 102 个红色的人。最早的线索才 16 天，94 人自己填了「30 天内要买」。

**2. 让销售养成点「🎯 合格 / 不合格」的习惯** —— 这是整条闭环的燃料。
线索流入 ~220/周，远超 Meta 学习期门槛（~50/周），**瓶颈不在广告量，在销售判定的执行率**。

**3. 等 `QualifiedLead` 事件积累几十条之后**：
Events Manager → Custom Conversions → 基于 `QualifiedLead` 建一个自定义转化 → 广告组优化目标设成它。
⚠️ **顺序不能反** —— 自定义事件必须先发出去、Meta 收到了才会出现在下拉列表里。
⚠️ **不要设 Purchase 当优化目标** —— 实测成交 0.8/周且全是 AI 误判，真实约 0，差学习期门槛两个数量级。

**4. 23 个成交待复核** —— 清单在 `待复核成交_2026-08-20.md`，一个都还没处理。

## 五、还没做的

- **两个 Edge Function 的改动没部署**（去掉旧 pixel 硬编码兜底 + Purchase 强制 currency）。都只是防呆，不影响当前功能。这台机器没有 supabase CLI，`npx supabase login` 之后 `npx supabase functions deploy conversions-api` 即可
- **`wa-cloud-webhook` / `ctwa_clid` 那条线动不了** —— 实测两个 business 里**都没有 WhatsApp Business Account**，你们用的是手机端 Business App 不是 Cloud API。要做 coexistence 得先把号码接入 Cloud API，是独立工程，不是配置问题
- **秦 Plus 520 人名单**还在等单价和有效期
- **git 身份**仍是 `Yang <yang@Yangs-Mac-mini.local>`，GitHub 上不关联账号

## 六、踩过的坑（别重犯）

1. **System user token 默认 60 天，必须手动选 Never** —— 连续踩两次。过期后 CAPI 和 lead webhook 同时静默失效。
2. **Meta webhook 是两层订阅**，App Dashboard 只做第一层，Page 层要 `POST /{page-id}/subscribed_apps`，新版后台无界面。
3. **自定义 CAPI 事件不能预先注册**，必须先发再建 Custom Conversion。
4. **回填手机号要按表单国家补区号**，`078xxx` 直接存成 `+078...` 是废号。
5. **`direction='inbound'` 过滤不够**（承 8/19），6 月方向判反的老行还没自愈，分析时还要看内容口气。
