# 交接：2026-08-19 这一天做了什么、还剩什么

> 新会话先读这份。所有数字都是当天实测的，不用重新跑。

## 一、已提交（本地，**尚未 push**）

```
110b7e4  客户卡加「客户在等你给什么」：自动认出索取 → 一键变待办
4ecafba  修上条：只认我方最后回复之后提的，噪音 23% → 2%
（本次）  成交只能人工确认：AI 永不许标 won
```

已 push 的（093c749 及之前）：lead watcher 去 N+1、备份导入修 lid+去重、
「我该回」改用 IndexedDB、wa-cloud-webhook、广告分析报告。

## 二、boss 最新定的口径（本次改动的由来）

**客户发来水单（银行回单）才算成交。** 其余一律不算。

起因：boss 发现有几个没成交的被判成了成交。实测确认
Karim (+237671205883) 库里是 `won`，但聊天里他最后两句是
「The price is a little high can you bring it lower」「How much will it
cost to me all in total」—— 还在砍价。根因是 Gem/Claude 回复里解析出的
`won/closed/closed_won` 被直接落库。

**本次已做**：
- `useAutoFbStage.isStageTransitionAllowed` 硬拦 `next === 'won'`
- `GemReplySection` / `ClaudeReplySection` 的 stage 映射 won→quoted
- 顺带取消 AI 的 `lost → won` 复活权

**还没做（下一步）**：
1. 客户卡加一个「✅ 已收水单」按钮 —— 唯一能置 `won` 的入口。
   点击时：置 stage=won + 记录时间 + 写 contact_events + 提示上传水单图片
   （vehicle_media / Cloudinary 已通，可复用）
2. `contact_sales_signals.payment_received_at` 的正则是坏的 ——
   匹配否定句（全库只命中 5 条，全是「we still have **not** received
   the payment」）。要么修正则加否定排除，要么直接废掉这一列，
   改用上面的人工水单时间。
3. 回头清洗历史：现有 24 个 `won` 里有多少是 AI 误判的，需要人工过一遍。
   建议做个「待复核成交」列表让 boss 逐个确认。

## 三、今天查实的关键数字（别重新跑）

**数据规模**：messages 118,506（今天从 59,998 补上来的）· contacts 9,121

**漏斗**：3,936 个客户收到过报价 → 36 个口头确认 → 24 个标记成交。
成交率约 1%，且 24 单**全部**来自转介绍 / 复购 / 长期关系，
标签里是 `trust_building` / `repeat buyer via Paul` / `6已成单-复购维护`。

**广告**：两个账户、六个国家、600+ 条付费线索、**0 成交**。
新账户 $262.78 / 454 条线索，RW/PL/AZ 各 0 单。
5 个广告组**全部广泛定向**，零兴趣条件；优化目标是 LEAD_GENERATION（表单提交）。
`contacts.ctwa_clid` 全库为 0 —— 归因链断裂。
⚠️ Brian 的时间轴显示 Meta 事件 Lead/InitiateCheckout **全部「失败 400」**，
conversions-api 其实一直没成功，待查。

**秦 Plus 二手**（boss 最关心的）：2024 款 / 实测 16,500km（不是 30,000）/
55KM 荣耀版 / 剩 6 台。成交率 0.34%，而 UNI-K 是 0.33%、CS75 Plus 1.43%、
RAV4 2.59% —— **不是这台车特别卖不掉，是整本簿子就 1%**。
按 0.34% 卖掉 6 台需要约 1,800 个有兴趣客户，实际只推销了 556 个。
报价混乱：$7,000 / $7,800 / $7,900 / $8,800 / $8,888 / $9,300 FOB。
运费是杀手：喀麦隆 车 $7,300 + 运费 $5,450 = 运费占车价 75%。
已生成 `秦Plus整批清仓_名单_2026-08-19.xlsx`（520 人分三层）+ 中英法西话术，
**等 boss 定单价和有效期就能发**。

## 四、数据质量的坑（新会话务必避开）

1. **搜消息必须分方向**。`text ilike '*qin*'` 有 947 条是我方群发推销，
   算成「客户有兴趣」会虚高一倍。
2. **广告表单自动首条消息含车名**（"How will you use the BYD Qin Plus DM-i?"），
   会被误判成客户主动提及。
3. **广告推送文案**（`Grow Your EV Business` / `logo-facebook-round`）会污染正则。
4. **2026-03~06 有 705 行引用块污染**（入站消息以「你」开头接英文话术）。
   该 DOM bug 已于 6 月修复，之后零污染；历史数据没清，分析时加
   `text not like '你%'`。
5. **WhatsApp Web 本地只有 2026-05-21 之后的消息**，冷线索在 Web 端搜不到，
   只能上手机。读聊天时 `get_page_text` 会截断，**别拿中间一段当结尾**——
   我今天就是这么把 Brian 误判成「客户要的东西没给到」的（实际他全回了）。

## 五、其他待办

见 `待办_未决事项.md`。重点未决：
- `package.mjs` 加 git 未提交拦截（提过三次未定）
- David 那台手机的 crypt15 备份（boss 说不管了）
- coexistence：BSP 选型（WhatsApp Web 已确认是受支持的 companion device）
- `quotes` 表全库 1 条 —— 报价没记录，价格一致性无从保证
