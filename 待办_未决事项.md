# 未决事项 / 技术债

> 记录时间：2026-08-19。按优先级排，做完一条划掉一条。

## 一、工程流程（8/18 丢代码事件的后续）

- [ ] **`extension/scripts/package.mjs` 加 git 未提交拦截**（我提过两次，未决）
  - 现状：`npm run package` 会把 `app_config.required_version` upsert 到 Supabase，
    等于一次「全公司发布」—— 但它完全不检查代码有没有进 git。
  - 8/18 就是这么发布了一个 git 里根本不存在的版本，靠 sourcemap 才捞回来。
  - 建议：第 1 步之前跑 `git status --porcelain`，非空则中断并提示先提交。
  - 守卫写进仓库后，要 David pull 到才在他机器生效 —— 而他正是打包的人。

- [ ] **git 身份未配（yang 这台机器）**
  - 提交 47e6d76 的 committer 是 `Yang <yang@Yangs-Mac-mini.local>`，
    主机名自动生成，GitHub 上不关联账号。
  - `git config --global user.name/user.email` 配一下。

- [ ] **`.gitignore` 的 `分析导出/` 匹配不到 `关税分析导出/`**
  - gitignore 不做子串匹配。改成 `*分析导出/`。

- [ ] **`广告与线索转化分析_2026-08.md` 仍未跟踪**，要不要进仓库待定。

## 二、已定位（2026-08-19 排查完成）

- [x] **【P0-阻塞】重跑 crypt15 导入前必须先改 `wa_message_id` 方案** —— 已修（cbe3e6f）
  - 实测：导入尝试 89,460 条，17,179 条被 key_id 去重挡掉（不修就是 17,179 条重复行）。
  - 两条路径 id 不同源，唯一约束 `(contact_id, wa_message_id)` 撞不上：
    ```
    DOM     :  AC1B15F33B76D385C200D001A6B14264   ← WhatsApp 原始 id
    crypt15 :  crypt15:<sha16(ts|dir|text)>       ← 自己算的 hash
    ```
  - 实测抽样 60 客户 / 1468 条，已存在 5 条同一消息被两种来源各存一份。
  - **暴露面：DOM 来源、2026-05-08 之后的消息共 28,006 条**（占全库 47%）。
    新备份覆盖到今天，直接导入 → 这 28,006 条每条多一份副本。
    其中 5,733 条 `sent_at` 为 NULL，事后连按「同时间同正文」去重都做不了。
  - 修法：`wa-backup-extract.ts` 的 SQL 加取 `message.key_id`
    （现在只取 `from_me, text_data, timestamp, message_type`），
    `wa-backup-import.ts` 用 `key_id` 当 `wa_message_id`，
    靠现有唯一约束天然去重。`ignoreDuplicates: true` 已在用 →
    已存在的 DOM 行胜出，`ai_source` 归因不会被覆盖。
  - 遗留：已有的 13,821 条 `crypt15:` 前缀行是旧方案。若新备份的时间范围
    确实覆盖 ≤2025-08-11，可在导入前删掉这批（会被新 id 完整重建）；
    否则留着，接受旧时段少量重复。**删除前需确认预览页的时间范围。**

- [x] **【P0】消息只在「人打开聊天」时才入库** —— 已用两招解决
  - ① 备份导入（cbe3e6f + 2026-08-19 执行）：messages 59,998 → 132,279，contacts 8,253 → 9,099。
    近 7 天覆盖率 77% 客户/54% 消息 → 124%/129%（DB 已是本机 Web 缓存的超集）。
  - ② 「我该回」改从 IDB 算（80b6693）：面板实测 **8 → 107**；
    顺带 34 个客户从「已流失」修正回「长期未联系」（历史保护规则终于拿到数据）。
  - 遗留（下同）：David 那台手机的备份还没跑。
- [ ] **~~原条目~~ 消息只在「人打开聊天」时才入库，且只读 DOM 最多 30 条**
  - 根因：`useMessageSync.ts:99` → `readChatMessages(30)`，绑定当前打开的聊天。
    所有 syncMessages 调用方（ReplySection / auto-reply / bulk-extract）都一样，
    **没有任何后台全量同步**。
  - 实测（近 7 天，yang 机器）：本机 IDB 202 客户 / 2499 条，库里 155 客户 / 1361 条。
    118 个客户库里一条没有，64 个偏少，仅 20 个一致。**约 1717 条未入库（69%）**。
  - 连带：5730 条 `sent_at = NULL`（占 9.6%）也是因为读 DOM——媒体气泡没有可解析时间。
  - ⚠️ 补 sent_at 的可行性已实测：id 关联方式正确（IDB msgId 第三段 = DB wa_message_id，
    样本时间戳全部对上），但**覆盖率仅 8.7%**（抽样 300 条只匹配 26 条）——
    本机 IDB 只有 7477 条而库里 59985 条，DOM 同步进去的历史 WhatsApp 本地早已清理。
    只能缓解，不能解决。每台销售机器能补的是不同的切片。
  - ❌ 原修法（从 IDB 批量入库）**不可行**：WhatsApp Web 已不在 IndexedDB 明文存正文，
    本机 7479 条消息 `body` 字段存在数 = 0（正文加密在 msgRowOpaqueData）。
    IDB 只能提供 msgId / chatId / 方向 / 精确时间戳，唯独没有文本。
  - ✅ 正确解法：**再跑一次手机加密备份导入**（管线现成：ImportBackupModal →
    wa-backup-decrypt → wa-backup-extract → wa-backup-import）。
    2026-05-09 跑过一次，导入 13,821 条、覆盖 2025-08-11 → 2026-05-08，
    且这批 `sent_at` NULL 数 = **0**（SQLite 时间戳精确）。
    2026-05-09 至今的缺口正是纯 DOM 抓取那段 —— 重跑一次即可补齐。
    两个 WhatsApp 号各跑一次（boss +8615555172187 / David 的号）。
  - ⚠️ 阻塞点 1：仅 Android（crypt15 是安卓本地/Google Drive 备份格式），iPhone 不适用。
  - ⚠️ 阻塞点 2：`wa-backup-extract.ts` 只取 `j.server='s.whatsapp.net'`，
    **`server='lid'` 的聊天被跳过**（注释写明「业务号 lid / 群 g.us 第一版跳过」）。
    而 WhatsApp 已全量迁移到 LID —— 新备份里可能大部分聊天都在 lid 下。
    好消息：summarizeBackup 会统计并显示 `lidChats`，导入前就能看到严重程度。
    若数量大，需要先扩展 extractor 做 lid → phone 解析。
  - ❌ 「用 jump-to-chat 程序化逐个打开聊天」的方案作废——备份导入更快更干净。
  - 🔴 连带发现：`lead-detector.ts:42` `if (!text || text.length < 40) return false`，
    而路径 B 传进去的 body 恒为空 —— **后台 lead 识别其实一直没命中过**。
    656f0d8 只把成本降到 0，功能是否重做待定。

- [x] **【P0】`useLeadDetector` 后台扫描 N+1** —— 已修（656f0d8），实测验证通过
  - 实测：4 分钟窗口（约 5 个扫描周期）修复前预期 ~315 请求，修复后 **1 请求**（-99.7%）。
    抓到的正是 `contacts?select=id,phone&phone=in.(63 个号码)` 批量查，门控挡掉其余周期。
  - `useLeadDetector.ts:222` 的 `ensureContact` 逐个 chat 发一次
    `contacts?select=id&phone=eq.X`，无缓存。实测一轮 **63 个请求**，
    `SCAN_INTERVAL_MS = 45s` → **约 12 万次请求/天/人**，3-4 个销售 = 36-48 万/天。
  - 修法：收集 phones 一次 `.in('phone', ...)` 批量查；复用 `jid-phone-cache.ts`；
    扫描前加指纹门控（latestByChat 没变就整轮跳过）。

- [ ] **【P1】`vehicle_interests` 43,455 行中 88.1% 是重复**
  - 去重后应为 5,159 行，可删 38,296 行，单条最多重复 1,476 次。
  - `useCrmData.ts:306` 的 `fetchAllVehicleInterests` 每小时全表分页 = 44 个请求，
    清完降到 6 个。
  - 8/18 已在应用层修了去重 key（97.4% → 1.9%），但**数据库层没有唯一约束兜底**：
    `create unique index on vehicle_interests (contact_id, lower(trim(model)));`
  - ⚠️ 删数据前需确认保留哪条（建议留 id 最小 = 最早创建的）。

- [ ] **【P2】WhatsApp 标签被另一个客户端来回覆盖**
  - 30 分钟采样实证（2026-08-19，只列 label id 6/7，user ecca2247 = owner）：
    ```
    07:19:30  6=岛国   7=mini    ← boss 机器（刚装扩展）写入，与本机 IDB 一致
    07:27:02  6=重要   7=跟进    ← 另一个客户端覆盖，是一套完全不同的标签
    07:55:55  6=岛国   7=mini    ← boss 机器重载扩展后又写回
    07:55 → 08:06  稳定不变（label-sync 的指纹门控生效，各自 24h 才重同步）
    ```
  - 被覆盖成的那套 id 1-5 = `新顾客/新订单/待付款/已支付/下单完成`，
    正是 WhatsApp Business 出厂默认标签；而本机真实标签是
    `大客户/有潜力/坦克500 准户/坦克700/卡罗拉`。id 9-19 两套完全一致。
  - 本机 WhatsApp 账号：`last-wid-md = 8615555172187:38@c.us`（+86 155 5517 2187，
    显示名 "Sino Gear Miles"），`WALid = 83718173630664:38@lid`。
  - 根因判断：**两台机器用同一个 CRM 账号（ecca2247 / owner）登录，但各自挂着
    不同的 WhatsApp 账号**。`whatsapp_labels` 唯一键是
    `(org_id, user_id, wa_label_id)`（0033），没有区分 WhatsApp 账号 →
    谁最后同步，库里就是谁的标签。**待你确认 David 那台是不是也用 owner 账号登录。**
  - 修法：迁移加一列存 WhatsApp 账号标识（从 IDB `wawc` → `user` 读
    `last-wid-md` / `WALid`），唯一键改成
    `(org_id, user_id, wa_account, wa_label_id)`。
